# Default URL for Postgres when `db` from docker-compose exposes 5432 on the host.
# Usage: npm run db:push:docker  (after docker compose up)
DATABASE_URL=postgres://forest:forest@127.0.0.1:5432/forest
