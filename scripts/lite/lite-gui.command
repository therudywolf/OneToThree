#!/usr/bin/env bash
# OneToThree Lite — graphical setup wizard (macOS, double-clickable).
# Opens the same setup as install.command, in a browser, with real checkboxes.
cd "$(dirname "$0")"
exec bash ./lite-gui.sh
